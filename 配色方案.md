# Fishbuddy 全局背景配色与视觉层级规范

本文档旨在解决当前 Fishbuddy 各核心模块（`today`、`time-management`、`habit`、`lists`、`daily-review`）背景色混用（如硬编码 `bg-[#f5f5f5]`、`bg-slate-50/70`、`bg-slate-950/80` 与语义化 `bg-card` / `bg-background` 混合）导致的页面视觉不统一问题。

---

## 1. 痛点分析与现状诊断

目前各模块背景色差异主要体现在：
- **混合使用硬编码类与语义 Token**：如 `AppLayout` 使用 `bg-[#f5f5f5]`，`TimeManagementPanel` 使用 `bg-slate-50/70 dark:bg-slate-950/80`，`HabitPanel` 使用 `bg-background`。
- **卡片与画布界限模糊**：在暗色模式下，部分面板的主背景比内部卡片还暗（如 `slate-950` vs `slate-900`），破坏了标准的“底板 -> 面板 -> 卡片”堆叠视觉深度。
- **分割线与阴影粗细不一**：某些模块使用 `border-border`，某些使用 `border-slate-200/80`，某些缺乏渐变区分。

---

## 2. 统一色彩视觉层级架构 (5-Level Elevation)

全局统一采用 5 级色彩深度架构，确保深浅色模式下具备一致的视沉浸感与层次区分：

| 视觉层级 | 层级名称 | 浅色模式 (Light Mode) | 暗色模式 (Dark Mode) | 适用组件 / 区域 |
| :--- | :--- | :--- | :--- | :--- |
| **Level 0 (L0)** | **应用窗口外壳 (Chrome Shell)** | `#f3f4f6` (Slate-100/80) | `#0f172a` (Slate-900) | 侧边栏 Toolbar、顶部 MenuBar 统一基底 |
| **Level 1 (L1)** | **主视图画布 (Viewport Canvas)** | `#f8fafc` (Slate-50) | `#020617` (Slate-950) | `<main>` 主内容容器底色 |
| **Level 2 (L2)** | **卡片/容器表面 (Surface Card)** | `#ffffff` (Pure White) | `#0f172a` (Slate-900) | 任务卡片、统计面板、四象限区块、笔记列表卡片 |
| **Level 3 (L3)** | **嵌套子组件/高亮块 (Sub-Card)** | `#f1f5f9` (Slate-100) | `#1e293b` (Slate-800) | 卡片内部项、悬浮态 (Hover)、已完成项背景 |
| **Level 4 (L4)** | **浮层弹窗/下拉菜单 (Popover)** | `#ffffff` (Pure White) | `#1e293b` (Slate-800) | 下拉菜单、快捷编辑框、Tooltip、对话框 |

---

## 3. 各模块背景色统一规范对照表

### 3.1 `AppLayout` (应用框架)
- **外壳 (Toolbar & MenuBar)**: 统一 `bg-[#f3f4f6] dark:bg-slate-900`
- **无缝连接处**: 保持无 border 分割
- **主内容容器 (`<main>`)**: 
  - `bg-slate-50 dark:bg-slate-950`
  - `rounded-tl-xl` (12px 小圆角)
  - `border-t border-l border-slate-200/80 dark:border-slate-800/80`

### 3.2 `today` (当日待办)
- **画布背景**: 继承 `<main>` 的 `bg-slate-50 dark:bg-slate-950`
- **卡片样式**: `bg-white dark:bg-slate-900 border border-slate-200/80 dark:border-slate-800 shadow-xs rounded-xl`
- **进度条背景**: `bg-slate-100 dark:bg-slate-800`

### 3.3 `time-management` (四象限工作台)
- **画布背景**: `bg-slate-50 dark:bg-slate-950` (移除 ad-hoc 透明度 `/70` 与 `/80`)
- **顶部次级 Header**: `bg-white/90 dark:bg-slate-900/90 border-b border-slate-200/80 dark:border-slate-800`
- **四象限网格区块**: `bg-white dark:bg-slate-900 rounded-xl border border-slate-200/80 dark:border-slate-800`

### 3.4 `habit` (习惯追踪)
- **画布背景**: `bg-slate-50 dark:bg-slate-950`
- **数据统计卡片**: `bg-white dark:bg-slate-900 rounded-xl border border-slate-200/80 dark:border-slate-800`
- **打卡热力图/表格头部**: `bg-slate-100/80 dark:bg-slate-800/60`

### 3.5 `lists` (知识库与笔记)
- **画布背景**: `bg-slate-50 dark:bg-slate-950`
- **笔记左侧树状导航栏**: `bg-white dark:bg-slate-900/90 border-r border-slate-200/80 dark:border-slate-800`
- **笔记主列表与卡片**: `bg-white dark:bg-slate-900 rounded-xl border border-slate-200/80 dark:border-slate-800`

### 3.6 `daily-review` (每日复盘)
- **画布背景**: `bg-slate-50 dark:bg-slate-950`
- **复盘编辑/查看区**: `bg-white dark:bg-slate-900 rounded-xl border border-slate-200/80 dark:border-slate-800 shadow-xs`

---

## 4. Tailwind 语义化 Class 使用指南

为保证后续开发不再引入背景色不统一问题，制定以下 Class 使用法则：

1. **主背景规范**: 统一使用 `bg-slate-50 dark:bg-slate-950` 作为页面级背景。
2. **卡片背景规范**: 统一使用 `bg-white dark:bg-slate-900` 作为卡片级背景。
3. **边框规范**: 统一使用 `border-slate-200/80 dark:border-slate-800`。
4. **悬浮与交互背景**: 统一使用 `hover:bg-slate-100 dark:hover:bg-slate-800/70`。
5. **禁用透明度散乱规则**: 禁止在根容器使用非标透明度如 `bg-slate-50/70` 或 `dark:bg-slate-950/80`，确保颜色纯正无混浊感。

---

## 5. 重构步骤规划

1. **第 1 阶段**：确认本文档规范并对其进行版本锁定。
2. **第 2 阶段**：调整 `AppLayout.tsx` 页面级容器基座背景与圆角。
3. **第 3 阶段**：逐一重构 `today`、`time-management`、`habit`、`lists`、`daily-review` 模块根容器背景色与卡片类名。
4. **第 4 阶段**：整体二次校验与暗色模式效果验收。
