# Tasks: 今日工作台智能逾期任务收敛与就地极速规划

## 1. 任务过滤与逾期判定逻辑增强

- [x] 1.1 在 `src/lib/taskSchedule.ts` 中实现 `isTaskOverdue` 与 `taskBelongsToToday` 辅助函数，确保历史未完成任务被纳入今日待办集合，并导出逾期天数/小时格式化工具。
- [x] 1.2 增强 `sortTasksByQuadrantAndDeadline` 排序逻辑，使未完成的逾期任务在时间流中置顶，在四象限模式中归入相应象限且置顶。


## 2. 今日工作台逾期任务视觉呈现与一键顺延

- [x] 2.1 在 `src/components/today/TodayPanel.tsx` 中更新任务筛选流，接入 `taskBelongsToToday`，计算 `overdueTasks` 数量与列表。
- [x] 2.2 在今日工作台顶部增加「⚠️ 逾期未完成」提示徽章与折叠区域，并在任务项右侧添加「📅 顺延至今日」快捷操作按钮。
- [x] 2.3 实现一键顺延动作处理函数，调用 `updateTask` 将逾期任务的截止时间更新为今日 23:59:59。

## 3. 今日工作台内联极速录入栏 (Inline Quick Add)

- [x] 3.1 创建/实现今日极速录入组件，包含标题输入框、四象限快捷切换胶囊（🔥🌿⚡💧）与项目选择下拉菜单（可选关联未归档项目）。
- [x] 3.2 在 `TodayPanel.tsx` 任务列表与四象限视图顶部集成该极速录入栏，支持按 `Enter` 键调用 `addTask` 极速创建并自动排期为今日。
- [x] 3.3 适配双主题（现代极简与复古像素 8-Bit）的外观样式与键盘交互（提交后自动清空并保持焦点）。


## 4. 全量验证与构建验收

- [x] 4.1 运行 `pnpm build` 确认 TypeScript 类型检查零错误且前端打包成功。
- [x] 4.2 验证逾期未完成任务正常展示、一键顺延即时生效、今日极速录入任务成功落库并同步展示。

