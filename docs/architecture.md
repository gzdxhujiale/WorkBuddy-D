# Architecture

The canonical system map is the repository-root `ARCHITECTURE.md`. It remains at the root so agents can discover it before entering the documentation site; this page exists only to provide the `/architecture` documentation route.
